/**
 * DashboardPage — stub.
 * Full implementation (task list, status summary, create form) added in Task 16.1.
 */

/** Route: /dashboard (protected) */
function DashboardPage() {
  return <div>Dashboard Page (Task 16.1)</div>
}

export default DashboardPage
