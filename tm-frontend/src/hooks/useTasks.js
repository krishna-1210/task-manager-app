/**
 * useTasks hook — stub.
 * Full implementation (task list state, CRUD actions) added in Task 14.1.
 */

// Stub export — replaced in Task 14.1
export function useTasks() {
  throw new Error('useTasks must be used within a component tree that has loaded tasks (implemented in Task 14.1)')
}
